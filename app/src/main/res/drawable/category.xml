<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The color item in the background -->
    <item android:drawable="@color/three" />

    <!-- The drawable item with image -->
    <item
        android:drawable="@drawable/sscategory"
        android:width="20dp"
        android:height="20dp" />
</layer-list>
